# Mediator Pattern

Mediator pattern is a behavioral design pattern that centralizes communication between components, so they interact through a mediator instead of holding direct references to each other. This reduces O(n²) dependencies to O(n) and keeps coordination logic in one place.

* [Mediator vs Observer pattern](https://stackoverflow.com/questions/9226479/mediator-vs-observer-object-oriented-design-patterns)
